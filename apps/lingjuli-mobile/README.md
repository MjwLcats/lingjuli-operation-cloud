# Lingjuli Mobile

uni-app x 移动执行端，使用 HBuilderX 打开。目标平台为 iOS、Android、HarmonyOS NEXT 与 H5。

业务源码按 `pages / modules / design-system / services / platform / stores / composables / models` 组织。`unpackage` 为构建产物，禁止编辑或提交。
