server.port: ${SERVER_PORT:9213}
spring:
  application.name: lingjuli-attendance-service
  datasource: {url: '${MYSQL_URL:jdbc:mysql://127.0.0.1:3306/lingjuli_attendance?serverTimezone=Asia/Shanghai}', username: '${MYSQL_USERNAME:root}', password: '${MYSQL_PASSWORD:}'}
  flyway.enabled: ${FLYWAY_ENABLED:true}
  cloud.nacos.discovery.enabled: ${NACOS_DISCOVERY_ENABLED:false}
management.endpoints.web.exposure.include: health,info,prometheus
